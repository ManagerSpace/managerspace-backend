name: 🚀 Deploy por FTPS a Hosting y NAS Synology

on:
  push:
    branches:
      - main

jobs:
  ftp-deploy:
    name: 📂 Subir archivos a Hosting y NAS Synology por FTPS
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Descargar el código
        uses: actions/checkout@v4

      # 🔹 Subir al Hosting principal
      - name: 📤 Subir archivos al Hosting (FTPS)
        uses: SamKirkland/FTP-Deploy-Action@v4.0.0
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}
          local-dir: ./
          server-dir: /managerspace/backend/
          protocol: ftps
          exclude: |
            **/.git*
            **/.github*
            **/node_modules/*
            **/*.md
          dry-run: false

      # 🔹 Subir al NAS Synology (FTPS)
      - name: 📤 Subir archivos al NAS Synology (FTPS)
        uses: SamKirkland/FTP-Deploy-Action@v4.0.0
        with:
          server: ${{ secrets.NAS_FTP_HOST }}
          username: ${{ secrets.NAS_FTP_USER }}
          password: ${{ secrets.NAS_FTP_PASS }}
          local-dir: ./
          server-dir: /GitHub/ManagerSpace/backend/
          protocol: ftps
          exclude: |
            **/.git*
            **/.github*
            **/node_modules/*
            **/*.md
          sync-mode: false
          dry-run: false
